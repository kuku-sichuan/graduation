各种图的应用与解释：
Scalar：（标量）
1. 在其图下会生成生成使用summary.scalar所属的name_scope以其名字命名的文件。
2. 最亮的线由smooth决定。
3. 每一个图的名字由其上层的name_scope的名字和自己的名字来决定。
Graphs:(网络图)
1. 图中展示每一种网络要素，如（输入，输出，网络结构等），同时也展示李各个
操作节点之间的tensor的大小。
Distributions and Histograms(分布和直方图)：
1. 有直方图就有分布图，其图下生成使用了histogram函数的name_scope名字的文件。
Embeddings:(嵌入或者理解成压缩)

在tensorboard中可以这么去理解T-SNE的可视化现象，因为T-SNE是一种非线性变换，
它把高维的数据转换成低维，保证数据在高维中的距离大，在低维中距离也大，在高
维中距离小，在低维中距离也小。
用T-SNE中可视化权重可以很好的体现每个输入的差异性（前提是输入归一化），如果
可视化出来的图分散的好，同时我们的结果也很好，说明我们权重生成的很棒，有效的
衡量了每个的输入的重要性，如果权重分布的很集中说明，我们的权重并没有很好的
每个输入的重要性。
用PCA分析如果权重没有被很好的PCA时，说明的权重很给力，如果我们的权重被PCA被
很好的PCA时，我们的结果就不是很很好了。（这里有待验证！）
